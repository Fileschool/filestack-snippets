/** @type {import('next').NextConfig} */
// Nothing Filestack-specific is needed here. v7 corrected its ESM and CJS export map,
// so the App Router resolves the package without a transpilePackages entry.
const nextConfig = {};

export default nextConfig;
